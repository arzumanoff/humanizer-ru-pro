[project]
name = "humanizer-ru-pro"
version = "0.1.0"
description = "Russian editorial skill and deterministic text audit"
requires-python = ">=3.10"
license = {text = "MIT"}

[tool.pytest.ini_options]
testpaths = ["tests"]
